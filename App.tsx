import React from 'react';
import {
  KeyboardAvoidingView,
  SafeAreaView,
  StyleSheet,
} from 'react-native';

function App(): React.JSX.Element {

  return (
    <SafeAreaView>
      <KeyboardAvoidingView>

      </KeyboardAvoidingView>
    </SafeAreaView>
  );
}

export default App;
